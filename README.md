# articles-ranker
* Install and start the API server
    - `cd backend`
    - `yarn install`
    - `yarn start`
* In another terminal window, install and start front-end
`
    - `cd frontend`
    - `yarn install`
    - `yarn start`

 # cypress tests
    - yarn cypress
    
# unit tests 
    - yarn test